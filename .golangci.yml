run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gofmt
    - goimports
    - govet
    - ineffassign
    - staticcheck
    - unused

issues:
  exclude-rules:
    - path: examples/
      linters:
        - errcheck
    - path: _test\.go
      linters:
        - errcheck
